# App
NODE_ENV=development
PORT=3001
API_URL=http://localhost:3001

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/telegram_saas?schema=public"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_SECRET=your-jwt-secret
JWT_REFRESH_SECRET=your-refresh-secret
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Encryption
ENCRYPTION_KEY=your-32-char-encryption-key

# Telegram
TELEGRAM_WEBHOOK_URL=https://telegram-saas-frontend.vercel.app/api/webhooks/telegram
# Enviado no setWebhook como secret_token e conferido em todo webhook recebido
# (header X-Telegram-Bot-Api-Secret-Token) — sem isso, qualquer um que descobrisse
# o botId podia simular clique/mensagem sem nunca ter usado o bot. Ausente = sem
# checagem (retrocompatível com bots já registrados antes dessa var existir).
TELEGRAM_WEBHOOK_SECRET=

# Essas duas variáveis não são lidas pelo código (não há seed automático) — o
# admin é só um User com role=ADMIN no banco. Mantidas aqui só como referência
# histórica do login local de dev; não refletem o admin real de produção.
ADMIN_EMAIL=admin@admin.com
ADMIN_PASSWORD=admin123

# BlackPay
BLACKPAY_API_URL=https://api.blackpay.com.br
BLACKPAY_API_KEY=your-blackpay-api-key
BLACKPAY_WEBHOOK_SECRET=your-blackpay-webhook-secret

# Web Push (PWA) — contato exigido pelo protocolo, visível só pros serviços de
# push dos navegadores (Google/Mozilla/Apple), nunca pro usuário final. Ausente =
# usa um fallback genérico no código; configure aqui pra refletir o domínio real.
VAPID_SUBJECT=mailto:suporte@seudominio.com

# Cadastro — CAPTCHA (Google reCAPTCHA v2) + verificação de e-mail (Resend)
# RECAPTCHA_SECRET_KEY ausente: verificação de captcha é pulada (fail-open).
# RESEND_API_KEY/RESEND_FROM_EMAIL ausentes: o cadastro FALHA (não dá pra
# mandar o código) — configure as três antes de subir em produção.
RECAPTCHA_SECRET_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=naoresponda@seudominio.com

# ── Módulo Marketing — Meta (Facebook) Marketing API ────────────────────────
# App do tipo Business em developers.facebook.com, produto "Marketing API".
# Redirect URI no App deve bater EXATAMENTE com META_OAUTH_REDIRECT_URI.
# Ausentes: o módulo Marketing aparece como "não configurado" e o resto do
# SaaS ignora — nada quebra.
META_APP_ID=
META_APP_SECRET=
META_OAUTH_REDIRECT_URI=https://api.seudominio.com/api/tracking/meta/oauth/callback
META_GRAPH_VERSION=v25.0
# Base pública do backend (fallback do redirect URI quando o de cima não é setado)
SERVER_PUBLIC_URL=https://api.seudominio.com
